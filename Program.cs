var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

app.Run();

// This is a minimal ASP.NET Core application that does nothing.
// It is designed to be a starting point for building web applications.